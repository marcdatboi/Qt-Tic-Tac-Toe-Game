



{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Main",
            "type": "shell",
            "command": "& cd ${workspaceFolder} && cmake --build build && ./build/tictactoe.exe",
            "group": "none",
            "presentation": {
                "reveal": "always",
                "panel": "new",
            }
        }
    ]
}